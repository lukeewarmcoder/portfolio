{
  "installCommand": "cd App/web && npm ci --include=dev",
  "buildCommand": "cd App/web && npm run build",
  "outputDirectory": "App/web/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
